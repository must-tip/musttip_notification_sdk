# TypeScript reference SDK

This dependency-free reference client runs on modern Node.js, Deno, Bun and browser runtimes with `fetch`, Web Crypto and WebSocket support. Browser applications must not receive management OAuth credentials; use the recipient ticket route for frontend realtime inboxes.
